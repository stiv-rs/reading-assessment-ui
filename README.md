# AI-Powered Reading Assessment Prototype

## Setup Instructions

### Frontend (Angular)

1. Navigate to the `frontend` folder.
2. Install dependencies:
   `npm install`
3. Start the development server:
   `npm start`
4. Open your browser and navigate to `http://localhost:4200`.
